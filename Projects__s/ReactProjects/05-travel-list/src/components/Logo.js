//If we export with named export, when importing, the variable name must be the same as the exports name. With default exports, it can be any name
export default function Logo() {
  return <h1>🌴 Far Away 🧳</h1>;
}
