Node list stuff
  //   let targetPassword;
  //   let targetUsername;
  //   e.target.childNodes.forEach((element) => {
  //     if (element.id === "username-input") targetUsername = element.id;
  //     if (element.id === "password-input") targetPassword = element.id;;
  //   });




  Text content stuff
  //.textContent to get text from elements



Inputs
//input_username.addEventListener("input", function (e) {
//   e.preventDefault();
//   if (input_username.value != "") 
// });

// input_password.addEventListener("input", function (e) {
//   e.preventDefault();
//   if (input_password.value != "") password = input_password.value;
// });


Using flex scroll
// To use flex scroll you must set a fixed with to your flex container if your flex-direction is row, but fixed height if your flex dire ction is column

No duality
// We can't use the comon js and ES6 module at the same time
// To use models add this to your package.json: "type": "module" 


app.js?
// I think we have to name our API file "app.js" for it to function


No DOM in node.js
//document relates to the DOM (Document Object Model) in a web browser. Node.js, however, is not a browser environment. It is a server environment, much like PHP or Perl, and as such, you can’t access the browser’s DOM or do anything specific to browser-hosted JavaScript.


